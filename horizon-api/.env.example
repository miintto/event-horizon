APP_ENV=local

DB_HOST=localhost
DB_PORT=5432
DB_NAME=horizon
DB_USER=horizon
DB_PASSWORD=horizon

INGEST_API_KEY=change-me

# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_ENCRYPTION_KEY=
# 로테이션 중에만 채운다 (기존 키). 재암호화가 끝나면 비운다.
SECRET_ENCRYPTION_KEY_PREVIOUS=
